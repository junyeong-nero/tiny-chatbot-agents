# 작업 로그 (2026-02-03)

## 📝 요약
오늘은 사용자가 RAG 파이프라인과 더 직관적으로 상호작용할 수 있도록 Streamlit 기반의 웹 인터페이스를 도입했습니다. 또한, 벡터 데이터베이스의 데이터 품질을 높이기 위해 중복 제거 로직을 구현하고, LLM 클라이언트의 라이브러리 호환성 문제를 해결했습니다.

## 🚀 주요 변경 사항

### 1. 웹 인터페이스 (Streamlit) 도입
- **대화형 UI 구현**: `streamlit_app.py`를 통해 터미널뿐만 아니라 웹 브라우저에서도 챗봇과 대화할 수 있는 환경 구축.
- **검색 및 설정 기능**:
    - RAG 파이프라인 채팅 테스트.
    - QnA 및 약관(ToS) 데이터베이스 개별 검색 기능.
    - 사이드바를 통한 LLM Provider 및 검증 임계값(Verification Threshold) 실시간 설정.
- **문서화**: `README.md`에 실행 방법 및 스크린샷(`assets/`) 추가.

### 2. Vector Store 최적화 (중복 제거)
- **QnA 스토어**: 질문(Question)과 답변(Answer) 내용을 기반으로 중복 입력을 방지하여 데이터 무결성 확보.
- **ToS 스토어**: 문서 제목, 섹션 제목, 내용을 기준으로 중복 청크가 저장되거나 검색 결과에 중복 노출되지 않도록 개선.
- **검색 품질 향상**: 검색 결과 리스트에서 동일한 내용이 반복되는 문제를 해결하여 사용자 경험 개선.

### 3. 시스템 안정성 개선
- **LLM 클라이언트 호환성 패치**: 최신 `httpx`와 `openai` 패키지 간의 프록시 설정 충돌을 방지하기 위해 `OpenAI` 클라이언트 초기화 시 명시적으로 `http_client`를 전달하도록 수정.

## 🔗 관련 커밋
- `37930e7`: feat(vectorstore): implement deduplication logic
- `8f6b5d5`: fix(llm): use explicit httpx client for OpenAI compatibility
- `6dd3224`: feat: add Streamlit web interface and documentation
